package ${package}.dao;

import ${package}.domain.${className}DO;

import java.util.List;
import java.util.Map;

import com.bootdo.common.dao.BaseDao;
import com.bootdo.common.domain.BaseDO;
import org.apache.ibatis.annotations.Mapper;

/**
 * ${comments}
 * @author ${author}
 * @date ${datetime}
 */
@Mapper
public interface ${className}Dao  extends BaseDao<T extends BaseDO> {

}
