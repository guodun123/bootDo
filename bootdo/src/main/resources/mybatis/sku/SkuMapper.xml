<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.bootdo.sku.dao.SkuDao">

    <select id="get" resultType="com.bootdo.sku.model.SkuDO">
		select `id`,`name`,`code`,`ean13`,`create_time` from md_sku where id = #{value}
	</select>

    <select id="listByMap" resultType="com.bootdo.sku.model.SkuDO">
        select `id`,`name`,`code`,`ean13`,`create_time` from md_sku
        <where>
            <if test="id != null and id != ''">and id =
                #{id}
            </if>
            <if test="name != null and name != ''">and name =
                #{name}
            </if>
            <if test="code != null and code != ''">and code =
                #{code}
            </if>
            <if test="ean13 != null and ean13 != ''">and ean13 =
                #{ean13}
            </if>
            <if test="createTime != null and createTime != ''">and create_time =
                #{createTime}
            </if>
        </where>
        <choose>
            <when test="sort != null and sort.trim() != ''">
                order by ${sort} ${order}
            </when>
            <otherwise>
                order by id desc
            </otherwise>
        </choose>
        <if test="offset != null and limit != null">
            limit #{offset}, #{limit}
        </if>
    </select>

    <select id="count" resultType="int">
        select count(*) from md_sku
        <where>
            <if test="id != null and id != ''">and id =
                #{id}
            </if>
            <if test="name != null and name != ''">and name =
                #{name}
            </if>
            <if test="code != null and code != ''">and code =
                #{code}
            </if>
            <if test="ean13 != null and ean13 != ''">and ean13 =
                #{ean13}
            </if>
            <if test="createTime != null and createTime != ''">and create_time =
                #{createTime}
            </if>
        </where>
    </select>

    <insert id="insert" parameterType="com.bootdo.sku.model.SkuDO" useGeneratedKeys="true" keyProperty="id">
        insert into md_sku
        <trim prefix="(" suffix=")" suffixOverrides=",">
            <if test="name != null and name != ''">
                `name`,
            </if>
            <if test="code != null and code != ''">
                `code`,
            </if>
            <if test="ean13 != null and ean13 != ''">
                `ean13`,
            </if>
            <if test="createTime != null and createTime != ''">
                `create_time`
            </if>
        </trim>
        <trim prefix="values (" suffix=")" suffixOverrides=",">
            <if test="name != null and name != ''">
                #{name},
            </if>
            <if test="code != null and code != ''">
                #{code},
            </if>
            <if test="ean13 != null and ean13 != ''">
                #{ean13},
            </if>
            <if test="createTime != null and createTime != ''">
                #{createTime}
            </if>
        </trim>
    </insert>

    <update id="update" parameterType="com.bootdo.sku.model.SkuDO">
        update md_sku
        <set>
            <if test="name != null and  name != ''">`name` =
                #{name
                },
            </if>
            <if test="code != null and  code != ''">
                `code` =#{code},
            </if>
            <if test="ean13 != null and  ean13 != ''">`ean13` =
                #{ean13
                },
            </if>
            <if test="createTime != null and  createTime != ''">`create_time` =
                #{createTime
                }
            </if>
        </set>
        where id = #{id}
    </update>

    <delete id="batchRemove">
        delete from md_sku where id in
        <foreach item="id" collection="array" open="(" separator="," close=")">
            #{id}
        </foreach>
    </delete>

</mapper>